# Proyecto aparte codigos

Este directorio guarda la extraccion de la mini-app de `codigos` que salio del proyecto principal.

Contenido movido:

- `app/codigos`
- `app/api/codigos`
- `lib/codes-*`
- `scripts/telegram-bridge.cjs`

Notas:

- El proyecto principal ahora muestra un aviso de mudanza en `/codigos`.
- `/chatgpt` fue eliminado del proyecto principal y no se movio aqui.
- Las recargas y el flujo de correos de Lemon siguen quedandose en el proyecto principal.
